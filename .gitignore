# Ingested telemetry state (prometheus metrics, loki logs, tempo traces).
/telemetry/

# Grafana runtime state — dashboards and alerts authored during the workshop.
/grafana/data/

# Local-only compose overrides (e.g. use locally built images).
/docker-compose.override.yml
