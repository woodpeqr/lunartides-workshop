apiVersion: 1

# No dashboards are provisioned. Students start with an empty Grafana and build
# their own panels and alert rules (REDESIGN_PLAN §5.1). Datasources remain
# provisioned so students don't fight datasource wiring.
providers: []
